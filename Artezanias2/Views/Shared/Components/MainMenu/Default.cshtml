@model IEnumerable<Page>
@foreach (var item in Model)
{
    @if (item.Slug != "inicio")
    {
        <li class="nav-item">
            <a class="nav-link text-black" asp-area="" asp-controller="Pages" asp-action="Page" asp-route-slug="@item.Slug">@item.Titulo</a>
        </li>
    }

}
